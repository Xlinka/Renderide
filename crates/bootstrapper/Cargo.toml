[package]
name = "bootstrapper"
version = "0.1.0"
edition = "2024"

[dependencies]
interprocess = { path = "../interprocess" }
logger = { path = "../logger" }
arboard = "3"
libc = "0.2"
serde_json = "1"