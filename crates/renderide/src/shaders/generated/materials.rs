//! @generated by UnityShaderConverter — do not edit.
//! Material structs and wgpu helpers derived from Unity ShaderLab Properties.

/// Unity shader: `Converter/MinimalUnlit`
/// Source: `UnityShaderConverter/SampleShaders/MinimalUnlit.shader`
pub mod converter_minimal_unlit {
    use glam::Vec4;

    /// Default material values mapped from the Unity Properties block.
    pub struct Material {
        pub color: Vec4, // Color
    }

    impl Default for Material {
        fn default() -> Self {
            Self {
                color: Vec4::new(1.0, 1.0, 1.0, 1.0),
            }
        }
    }

    /// Placeholder bind group layout entries for material uniforms (group 1) — extend as WGSL bindings are added.
    pub fn material_bind_group_layout_entries() -> &'static [wgpu::BindGroupLayoutEntry] {
        &[]
    }

    /// Creates an empty material bind group layout (until textures/uniforms are wired).
    pub fn create_material_bind_group_layout(device: &wgpu::Device, label: &str) -> wgpu::BindGroupLayout {
        device.create_bind_group_layout(&wgpu::BindGroupLayoutDescriptor {
            label: Some(label),
            entries: material_bind_group_layout_entries(),
        })
    }

    /// Single color attachment state for the given surface format.
    pub fn color_target_state(format: wgpu::TextureFormat) -> Option<wgpu::ColorTargetState> {
        Some(wgpu::ColorTargetState {
            format,
            blend: None,
            write_mask: wgpu::ColorWrites::ALL,
        })
    }

    /// Vertex shader entry name produced by Slang for this shader (pass 0).
    pub const VERTEX_ENTRY_PASS0: &str = "vert";
    pub const FRAGMENT_ENTRY_PASS0: &str = "frag";
}

