//! @generated by UnityShaderConverter — do not edit.
//! Unity shader: `Xiexe/Toon2.0/XSToon2.0_CutoutA2C`
//! Source: `third_party/Resonite.UnityShaders/Assets/Shaders/Xiexes/Main/Shaders/XSToon2.0 CutoutA2C.shader`
//! One WGSL file per pass (named from ShaderLab `Name` or `passN`).
//! WGSL `override` / `@id` constants use `wgpu::PipelineCompilationOptions` on vertex and fragment stages (wgpu 28).
//! Texture properties on `Material` use `TextureHandle` (default `0`); host defines invalid/unbound semantics.

#![allow(
    non_snake_case,
    unused_imports,
    unused_variables,
    clippy::derivable_impls,
    clippy::excessive_precision,
    clippy::ptr_arg,
)]

use glam::Vec4;
use crate::scene::types::TextureHandle;

/// Maps to WGSL `override` / `wgpu::PipelineCompilationOptions::constants` (decimal id keys per wgpu docs).
/// `Default` matches WGSL override initializer defaults from Slang `[vk::constant_id]` (first keyword true on exclusive `multi_compile` lines).
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash)]
pub struct VariantKey {
    /// ShaderLab keyword `VERTEXLIGHT_ON`
    pub vertexlight_on: bool,
}

impl Default for VariantKey {
    fn default() -> Self {
        Self {
            vertexlight_on: false,
        }
    }
}

/// WGSL clustered scene uniforms, lights, cluster buffers (matches builtin PBR).
pub const RENDERIDE_SCENE_BIND_GROUP: u32 = 1;
/// WGSL `MaterialUniform` and texture properties group index.
pub const MATERIAL_BIND_GROUP: u32 = 2;

/// Pushes WGSL `@group(1)` layout entries: scene uniform, light storage, cluster count/index buffers.
/// Matches [`crate::gpu::pipeline::PbrPipeline`](crate::gpu::pipeline::PbrPipeline) scene group.
#[allow(dead_code)]
pub fn push_renderide_scene_bind_group_layout_entries(entries: &mut Vec<wgpu::BindGroupLayoutEntry>) {
    use std::num::NonZeroU64;
    let scene_uniform_size = std::mem::size_of::<crate::gpu::pipeline::SceneUniforms>() as u64;
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 0,
        visibility: wgpu::ShaderStages::FRAGMENT,
        ty: wgpu::BindingType::Buffer {
            ty: wgpu::BufferBindingType::Uniform,
            has_dynamic_offset: false,
            min_binding_size: NonZeroU64::new(scene_uniform_size),
        },
        count: None,
    });
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 1,
        visibility: wgpu::ShaderStages::FRAGMENT,
        ty: wgpu::BindingType::Buffer {
            ty: wgpu::BufferBindingType::Storage { read_only: true },
            has_dynamic_offset: false,
            min_binding_size: None,
        },
        count: None,
    });
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 2,
        visibility: wgpu::ShaderStages::FRAGMENT,
        ty: wgpu::BindingType::Buffer {
            ty: wgpu::BufferBindingType::Storage { read_only: true },
            has_dynamic_offset: false,
            min_binding_size: None,
        },
        count: None,
    });
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 3,
        visibility: wgpu::ShaderStages::FRAGMENT,
        ty: wgpu::BindingType::Buffer {
            ty: wgpu::BufferBindingType::Storage { read_only: true },
            has_dynamic_offset: false,
            min_binding_size: None,
        },
        count: None,
    });
}

/// Merged vertex + fragment WGSL for pass 0.
pub const PASS0_WGSL: &str = include_str!("forward.wgsl");

/// Merged vertex + fragment WGSL for pass 1.
pub const PASS1_WGSL: &str = include_str!("fwdadd.wgsl");

/// Merged vertex + fragment WGSL for pass 2.
pub const PASS2_WGSL: &str = include_str!("shadow_caster.wgsl");

/// Default material values mapped from the Unity Properties block.
#[derive(Clone, Debug, PartialEq)]
pub struct Material {
    pub vertexColorAlbedo: i32, // Vertex Color Albedo
    pub tilingMode: i32, // Tiling Mode
    pub culling: i32, // Culling Mode
    pub mainTex: TextureHandle, // Texture
    pub saturation: f32, // Main Texture Saturation
    pub color: Vec4, // Color Tint
    pub cutoff: f32, // Cutoff
    pub bumpMap: TextureHandle, // Normal Map
    pub bumpScale: f32, // Normal Scale
    pub detailNormalMap: TextureHandle, // Detail Normal Map
    pub detailMask: TextureHandle, // Detail Mask
    pub detailNormalMapScale: f32, // Detail Normal Scale
    pub reflectionMode: i32, // Reflection Mode
    pub clearCoat: i32, // ClearCoat
    pub reflectionBlendMode: i32, // Reflection Blend Mode
    pub metallicGlossMap: TextureHandle, // Metallic
    pub bakedCubemap: TextureHandle, // Baked Cubemap
    pub matcap: TextureHandle, // Matcap
    pub matcapTint: Vec4, // Matcap Tint
    pub reflectivityMask: TextureHandle, // Reflection Mask
    pub metallic: f32, // Metallic
    pub glossiness: f32, // Smoothness
    pub reflectivity: f32, // Reflectivity
    pub clearcoatStrength: f32, // Clearcoat Reflectivity
    pub clearcoatSmoothness: f32, // Clearcoat Smoothness
    pub scaleWithLight: i32, // Emission Scale w/ Light
    pub emissionMap: TextureHandle, // Emission Map
    pub emissionColor: Vec4, // Emission Color
    pub emissionToDiffuse: f32, // Emission Tint To Diffuse
    pub scaleWithLightSensitivity: f32, // Scaling Sensitivity
    pub rimColor: Vec4, // Rimlight Tint
    pub rimAlbedoTint: f32, // Rim Albedo Tint
    pub rimCubemapTint: f32, // Rim Environment Tint
    pub rimAttenEffect: f32, // Rim Attenuation Effect
    pub rimIntensity: f32, // Rimlight Intensity
    pub rimRange: f32, // Rim Range
    pub rimThreshold: f32, // Rim Threshold
    pub rimSharpness: f32, // Rim Sharpness
    pub specMode: i32, // Specular Mode
    pub specularStyle: i32, // Specular Style
    pub specularMap: TextureHandle, // Specular Map
    pub specularIntensity: f32, // Specular Intensity
    pub specularArea: f32, // Specular Smoothness
    pub anisotropicAX: f32, // Anisotropic X
    pub anisotropicAY: f32, // Anisotripic Y
    pub specularAlbedoTint: f32, // Specular Albedo Tint
    pub rampSelectionMask: TextureHandle, // Ramp Mask
    pub ramp: TextureHandle, // Shadow Ramp
    pub shadowSharpness: f32, // Received Shadow Sharpness
    pub shadowRim: Vec4, // Shadow Rim Tint
    pub shadowRimRange: f32, // Shadow Rim Range
    pub shadowRimThreshold: f32, // Shadow Rim Threshold
    pub shadowRimSharpness: f32, // Shadow Rim Sharpness
    pub shadowRimAlbedoTint: f32, // Shadow Rim Albedo Tint
    pub occlusionMap: TextureHandle, // Occlusion
    pub occlusionColor: Vec4, // Occlusion Color
    pub outlineAlbedoTint: i32, // Outline Albedo Tint
    pub outlineLighting: i32, // Outline Lighting
    pub outlineMask: TextureHandle, // Outline Mask
    pub outlineWidth: f32, // Outline Width
    pub outlineColor: Vec4, // Outline Color
    pub thicknessMap: TextureHandle, // Thickness Map
    pub sSColor: Vec4, // Subsurface Color
    pub sSDistortion: f32, // Normal Distortion
    pub sSPower: f32, // Subsurface Power
    pub sSScale: f32, // Subsurface Scale
    pub halftoneDotSize: f32, // Halftone Dot Size
    pub halftoneDotAmount: f32, // Halftone Dot Amount
    pub halftoneLineAmount: f32, // Halftone Line Amount
    pub uVSetAlbedo: i32, // Albedo UVs
    pub uVSetNormal: i32, // Normal Map UVs
    pub uVSetDetNormal: i32, // Detail Normal UVs
    pub uVSetDetMask: i32, // Detail Mask UVs
    pub uVSetMetallic: i32, // Metallic Map UVs
    pub uVSetSpecular: i32, // Specular Map UVs
    pub uVSetReflectivity: i32, // Reflection Mask UVs
    pub uVSetThickness: i32, // Thickness Map UVs
    pub uVSetOcclusion: i32, // Occlusion Map UVs
    pub uVSetEmission: i32, // Emission Map UVs
    pub advMode: i32, // Shader Mode
    pub stencil: f32, // Stencil ID [0;255]
    pub stencilComp: i32, // Stencil Comparison
    pub stencilOp: i32, // Stencil Operation
}

impl Default for Material {
    fn default() -> Self {
        Self {
            vertexColorAlbedo: 0,
            tilingMode: 0,
            culling: 2,
            mainTex: 0,
            saturation: 1.0,
            color: Vec4::new(1.0, 1.0, 1.0, 1.0),
            cutoff: 0.5,
            bumpMap: 0,
            bumpScale: 1.0,
            detailNormalMap: 0,
            detailMask: 0,
            detailNormalMapScale: 1.0,
            reflectionMode: 3,
            clearCoat: 0,
            reflectionBlendMode: 0,
            metallicGlossMap: 0,
            bakedCubemap: 0,
            matcap: 0,
            matcapTint: Vec4::new(1.0, 1.0, 1.0, 1.0),
            reflectivityMask: 0,
            metallic: 0.0,
            glossiness: 0.0,
            reflectivity: 1.0,
            clearcoatStrength: 1.0,
            clearcoatSmoothness: 0.800000012,
            scaleWithLight: 1,
            emissionMap: 0,
            emissionColor: Vec4::new(0.0, 0.0, 0.0, 0.0),
            emissionToDiffuse: 0.0,
            scaleWithLightSensitivity: 1.0,
            rimColor: Vec4::new(1.0, 1.0, 1.0, 1.0),
            rimAlbedoTint: 0.0,
            rimCubemapTint: 0.0,
            rimAttenEffect: 1.0,
            rimIntensity: 0.0,
            rimRange: 0.699999988,
            rimThreshold: 0.100000001,
            rimSharpness: 0.100000001,
            specMode: 0,
            specularStyle: 0,
            specularMap: 0,
            specularIntensity: 0.0,
            specularArea: 0.5,
            anisotropicAX: 0.25,
            anisotropicAY: 0.75,
            specularAlbedoTint: 1.0,
            rampSelectionMask: 0,
            ramp: 0,
            shadowSharpness: 0.5,
            shadowRim: Vec4::new(1.0, 1.0, 1.0, 1.0),
            shadowRimRange: 0.699999988,
            shadowRimThreshold: 0.100000001,
            shadowRimSharpness: 0.300000012,
            shadowRimAlbedoTint: 0.0,
            occlusionMap: 0,
            occlusionColor: Vec4::new(0.0, 0.0, 0.0, 0.0),
            outlineAlbedoTint: 0,
            outlineLighting: 0,
            outlineMask: 0,
            outlineWidth: 1.0,
            outlineColor: Vec4::new(0.0, 0.0, 0.0, 1.0),
            thicknessMap: 0,
            sSColor: Vec4::new(0.0, 0.0, 0.0, 0.0),
            sSDistortion: 1.0,
            sSPower: 1.0,
            sSScale: 1.0,
            halftoneDotSize: 1.70000005,
            halftoneDotAmount: 50.0,
            halftoneLineAmount: 150.0,
            uVSetAlbedo: 0,
            uVSetNormal: 0,
            uVSetDetNormal: 0,
            uVSetDetMask: 0,
            uVSetMetallic: 0,
            uVSetSpecular: 0,
            uVSetReflectivity: 0,
            uVSetThickness: 0,
            uVSetOcclusion: 0,
            uVSetEmission: 0,
            advMode: 0,
            stencil: 0.0,
            stencilComp: 0,
            stencilOp: 0,
        }
    }
}

/// WGSL source for use with `Device::create_shader_module`.
pub fn shader_source_forward() -> wgpu::ShaderSource<'static> {
    wgpu::ShaderSource::Wgsl(PASS0_WGSL.into())
}

pub fn shader_source_fwdadd() -> wgpu::ShaderSource<'static> {
    wgpu::ShaderSource::Wgsl(PASS1_WGSL.into())
}

pub fn shader_source_shadow_caster() -> wgpu::ShaderSource<'static> {
    wgpu::ShaderSource::Wgsl(PASS2_WGSL.into())
}

static VERTEX_ATTRIBUTES_PASS0: &[wgpu::VertexAttribute] = &[
    wgpu::VertexAttribute { format: wgpu::VertexFormat::Float32x4, offset: 0, shader_location: 0 },
    wgpu::VertexAttribute { format: wgpu::VertexFormat::Float32x2, offset: 16, shader_location: 1 },
    wgpu::VertexAttribute { format: wgpu::VertexFormat::Float32x2, offset: 24, shader_location: 2 },
    wgpu::VertexAttribute { format: wgpu::VertexFormat::Float32x3, offset: 32, shader_location: 3 },
    wgpu::VertexAttribute { format: wgpu::VertexFormat::Float32x4, offset: 48, shader_location: 4 },
    wgpu::VertexAttribute { format: wgpu::VertexFormat::Float32x4, offset: 64, shader_location: 5 },
];
static VERTEX_BUFFER_LAYOUT_PASS0: [wgpu::VertexBufferLayout; 1] = [wgpu::VertexBufferLayout {
    array_stride: 80,
    step_mode: wgpu::VertexStepMode::Vertex,
    attributes: VERTEX_ATTRIBUTES_PASS0,
}];
pub static VERTEX_BUFFER_LAYOUTS_PASS0: &[wgpu::VertexBufferLayout] = &VERTEX_BUFFER_LAYOUT_PASS0;

static VERTEX_ATTRIBUTES_PASS1: &[wgpu::VertexAttribute] = &[
    wgpu::VertexAttribute { format: wgpu::VertexFormat::Float32x4, offset: 0, shader_location: 0 },
    wgpu::VertexAttribute { format: wgpu::VertexFormat::Float32x2, offset: 16, shader_location: 1 },
    wgpu::VertexAttribute { format: wgpu::VertexFormat::Float32x2, offset: 24, shader_location: 2 },
    wgpu::VertexAttribute { format: wgpu::VertexFormat::Float32x3, offset: 32, shader_location: 3 },
    wgpu::VertexAttribute { format: wgpu::VertexFormat::Float32x4, offset: 48, shader_location: 4 },
    wgpu::VertexAttribute { format: wgpu::VertexFormat::Float32x4, offset: 64, shader_location: 5 },
];
static VERTEX_BUFFER_LAYOUT_PASS1: [wgpu::VertexBufferLayout; 1] = [wgpu::VertexBufferLayout {
    array_stride: 80,
    step_mode: wgpu::VertexStepMode::Vertex,
    attributes: VERTEX_ATTRIBUTES_PASS1,
}];
pub static VERTEX_BUFFER_LAYOUTS_PASS1: &[wgpu::VertexBufferLayout] = &VERTEX_BUFFER_LAYOUT_PASS1;

static VERTEX_ATTRIBUTES_PASS2: &[wgpu::VertexAttribute] = &[
    wgpu::VertexAttribute { format: wgpu::VertexFormat::Float32x4, offset: 0, shader_location: 0 },
    wgpu::VertexAttribute { format: wgpu::VertexFormat::Float32x3, offset: 16, shader_location: 1 },
    wgpu::VertexAttribute { format: wgpu::VertexFormat::Float32x2, offset: 32, shader_location: 2 },
];
static VERTEX_BUFFER_LAYOUT_PASS2: [wgpu::VertexBufferLayout; 1] = [wgpu::VertexBufferLayout {
    array_stride: 40,
    step_mode: wgpu::VertexStepMode::Vertex,
    attributes: VERTEX_ATTRIBUTES_PASS2,
}];
pub static VERTEX_BUFFER_LAYOUTS_PASS2: &[wgpu::VertexBufferLayout] = &VERTEX_BUFFER_LAYOUT_PASS2;

/// Uniform buffer layout matching the prepended WGSL `MaterialUniform` (std140 with explicit padding).
#[repr(C, align(16))]
#[allow(dead_code)]
pub struct MaterialUniform {
    pub vertexColorAlbedo: i32,
    pub tilingMode: i32,
    pub culling: i32,
    pub saturation: f32,
    pub color: Vec4,
    pub cutoff: f32,
    pub bumpScale: f32,
    pub detailNormalMapScale: f32,
    pub reflectionMode: i32,
    pub clearCoat: i32,
    pub reflectionBlendMode: i32,
    pub _pad0: u32,
    pub _pad1: u32,
    pub matcapTint: Vec4,
    pub metallic: f32,
    pub glossiness: f32,
    pub reflectivity: f32,
    pub clearcoatStrength: f32,
    pub clearcoatSmoothness: f32,
    pub scaleWithLight: i32,
    pub _pad2: u32,
    pub _pad3: u32,
    pub emissionColor: Vec4,
    pub emissionToDiffuse: f32,
    pub scaleWithLightSensitivity: f32,
    pub _pad4: u32,
    pub _pad5: u32,
    pub rimColor: Vec4,
    pub rimAlbedoTint: f32,
    pub rimCubemapTint: f32,
    pub rimAttenEffect: f32,
    pub rimIntensity: f32,
    pub rimRange: f32,
    pub rimThreshold: f32,
    pub rimSharpness: f32,
    pub specMode: i32,
    pub specularStyle: i32,
    pub specularIntensity: f32,
    pub specularArea: f32,
    pub anisotropicAX: f32,
    pub anisotropicAY: f32,
    pub specularAlbedoTint: f32,
    pub shadowSharpness: f32,
    pub _pad6: u32,
    pub shadowRim: Vec4,
    pub shadowRimRange: f32,
    pub shadowRimThreshold: f32,
    pub shadowRimSharpness: f32,
    pub shadowRimAlbedoTint: f32,
    pub occlusionColor: Vec4,
    pub outlineAlbedoTint: i32,
    pub outlineLighting: i32,
    pub outlineWidth: f32,
    pub _pad7: u32,
    pub outlineColor: Vec4,
    pub sSColor: Vec4,
    pub sSDistortion: f32,
    pub sSPower: f32,
    pub sSScale: f32,
    pub halftoneDotSize: f32,
    pub halftoneDotAmount: f32,
    pub halftoneLineAmount: f32,
    pub uVSetAlbedo: i32,
    pub uVSetNormal: i32,
    pub uVSetDetNormal: i32,
    pub uVSetDetMask: i32,
    pub uVSetMetallic: i32,
    pub uVSetSpecular: i32,
    pub uVSetReflectivity: i32,
    pub uVSetThickness: i32,
    pub uVSetOcclusion: i32,
    pub uVSetEmission: i32,
    pub advMode: i32,
    pub stencil: f32,
    pub stencilComp: i32,
    pub stencilOp: i32,
}

/// Maps `VariantKey` fields to pipeline constant ids (decimal strings per WebGPU).
pub fn specialization_constants_f64(variant: &VariantKey) -> std::vec::Vec<(&'static str, f64)> {
    let mut v = std::vec::Vec::new();
    v.push(("0", if variant.vertexlight_on { 1.0 } else { 0.0 }));
    v
}

static SPECIALIZATION_KEYS: &[&str] = &[
    "0",
];

fn pipeline_compilation_options_vertex_inner(variant: &VariantKey) -> wgpu::PipelineCompilationOptions<'static> {
    let vals: [f64; 1] = [
        if variant.vertexlight_on { 1.0 } else { 0.0 },
    ];
    let tuples: std::vec::Vec<(&'static str, f64)> = SPECIALIZATION_KEYS
        .iter()
        .copied()
        .zip(vals)
        .collect();
    let boxed = tuples.into_boxed_slice();
    let leaked = Box::leak(boxed);
    wgpu::PipelineCompilationOptions {
        constants: leaked,
        ..Default::default()
    }
}

/// Builds `PipelineCompilationOptions` for vertex/fragment stages (WGSL `override` / `@id`).
/// Note: leaks a tiny slice per call; cache by `VariantKey` in hot paths.
/// Per-pass wrappers delegate to the same implementation (specialization is per-shader).
pub fn pipeline_compilation_options_pass0_vertex(variant: &VariantKey) -> wgpu::PipelineCompilationOptions<'static> {
    pipeline_compilation_options_vertex_inner(variant)
}

pub fn pipeline_compilation_options_pass0_fragment(variant: &VariantKey) -> wgpu::PipelineCompilationOptions<'static> {
    pipeline_compilation_options_vertex_inner(variant)
}

pub fn pipeline_compilation_options_pass1_vertex(variant: &VariantKey) -> wgpu::PipelineCompilationOptions<'static> {
    pipeline_compilation_options_vertex_inner(variant)
}

pub fn pipeline_compilation_options_pass1_fragment(variant: &VariantKey) -> wgpu::PipelineCompilationOptions<'static> {
    pipeline_compilation_options_vertex_inner(variant)
}

pub fn pipeline_compilation_options_pass2_vertex(variant: &VariantKey) -> wgpu::PipelineCompilationOptions<'static> {
    pipeline_compilation_options_vertex_inner(variant)
}

pub fn pipeline_compilation_options_pass2_fragment(variant: &VariantKey) -> wgpu::PipelineCompilationOptions<'static> {
    pipeline_compilation_options_vertex_inner(variant)
}

/// Bind group 2: `MaterialUniform` at binding 0, then each texture + sampler pair.
fn push_material_bind_group_layout_entries(entries: &mut Vec<wgpu::BindGroupLayoutEntry>) {
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 0,
        visibility: wgpu::ShaderStages::VERTEX_FRAGMENT,
        ty: wgpu::BindingType::Buffer {
            ty: wgpu::BufferBindingType::Uniform,
            has_dynamic_offset: false,
            min_binding_size: None,
        },
        count: None,
    });
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 1,
        visibility: wgpu::ShaderStages::FRAGMENT,
        ty: wgpu::BindingType::Texture {
            sample_type: wgpu::TextureSampleType::Float { filterable: true },
            view_dimension: wgpu::TextureViewDimension::D2,
            multisampled: false,
        },
        count: None,
    });
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 2,
        visibility: wgpu::ShaderStages::FRAGMENT,
        ty: wgpu::BindingType::Sampler(wgpu::SamplerBindingType::Filtering),
        count: None,
    });
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 3,
        visibility: wgpu::ShaderStages::FRAGMENT,
        ty: wgpu::BindingType::Texture {
            sample_type: wgpu::TextureSampleType::Float { filterable: true },
            view_dimension: wgpu::TextureViewDimension::D2,
            multisampled: false,
        },
        count: None,
    });
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 4,
        visibility: wgpu::ShaderStages::FRAGMENT,
        ty: wgpu::BindingType::Sampler(wgpu::SamplerBindingType::Filtering),
        count: None,
    });
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 5,
        visibility: wgpu::ShaderStages::FRAGMENT,
        ty: wgpu::BindingType::Texture {
            sample_type: wgpu::TextureSampleType::Float { filterable: true },
            view_dimension: wgpu::TextureViewDimension::D2,
            multisampled: false,
        },
        count: None,
    });
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 6,
        visibility: wgpu::ShaderStages::FRAGMENT,
        ty: wgpu::BindingType::Sampler(wgpu::SamplerBindingType::Filtering),
        count: None,
    });
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 7,
        visibility: wgpu::ShaderStages::FRAGMENT,
        ty: wgpu::BindingType::Texture {
            sample_type: wgpu::TextureSampleType::Float { filterable: true },
            view_dimension: wgpu::TextureViewDimension::D2,
            multisampled: false,
        },
        count: None,
    });
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 8,
        visibility: wgpu::ShaderStages::FRAGMENT,
        ty: wgpu::BindingType::Sampler(wgpu::SamplerBindingType::Filtering),
        count: None,
    });
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 9,
        visibility: wgpu::ShaderStages::FRAGMENT,
        ty: wgpu::BindingType::Texture {
            sample_type: wgpu::TextureSampleType::Float { filterable: true },
            view_dimension: wgpu::TextureViewDimension::D2,
            multisampled: false,
        },
        count: None,
    });
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 10,
        visibility: wgpu::ShaderStages::FRAGMENT,
        ty: wgpu::BindingType::Sampler(wgpu::SamplerBindingType::Filtering),
        count: None,
    });
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 11,
        visibility: wgpu::ShaderStages::FRAGMENT,
        ty: wgpu::BindingType::Texture {
            sample_type: wgpu::TextureSampleType::Float { filterable: true },
            view_dimension: wgpu::TextureViewDimension::Cube,
            multisampled: false,
        },
        count: None,
    });
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 12,
        visibility: wgpu::ShaderStages::FRAGMENT,
        ty: wgpu::BindingType::Sampler(wgpu::SamplerBindingType::Filtering),
        count: None,
    });
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 13,
        visibility: wgpu::ShaderStages::FRAGMENT,
        ty: wgpu::BindingType::Texture {
            sample_type: wgpu::TextureSampleType::Float { filterable: true },
            view_dimension: wgpu::TextureViewDimension::D2,
            multisampled: false,
        },
        count: None,
    });
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 14,
        visibility: wgpu::ShaderStages::FRAGMENT,
        ty: wgpu::BindingType::Sampler(wgpu::SamplerBindingType::Filtering),
        count: None,
    });
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 15,
        visibility: wgpu::ShaderStages::FRAGMENT,
        ty: wgpu::BindingType::Texture {
            sample_type: wgpu::TextureSampleType::Float { filterable: true },
            view_dimension: wgpu::TextureViewDimension::D2,
            multisampled: false,
        },
        count: None,
    });
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 16,
        visibility: wgpu::ShaderStages::FRAGMENT,
        ty: wgpu::BindingType::Sampler(wgpu::SamplerBindingType::Filtering),
        count: None,
    });
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 17,
        visibility: wgpu::ShaderStages::FRAGMENT,
        ty: wgpu::BindingType::Texture {
            sample_type: wgpu::TextureSampleType::Float { filterable: true },
            view_dimension: wgpu::TextureViewDimension::D2,
            multisampled: false,
        },
        count: None,
    });
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 18,
        visibility: wgpu::ShaderStages::FRAGMENT,
        ty: wgpu::BindingType::Sampler(wgpu::SamplerBindingType::Filtering),
        count: None,
    });
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 19,
        visibility: wgpu::ShaderStages::FRAGMENT,
        ty: wgpu::BindingType::Texture {
            sample_type: wgpu::TextureSampleType::Float { filterable: true },
            view_dimension: wgpu::TextureViewDimension::D2,
            multisampled: false,
        },
        count: None,
    });
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 20,
        visibility: wgpu::ShaderStages::FRAGMENT,
        ty: wgpu::BindingType::Sampler(wgpu::SamplerBindingType::Filtering),
        count: None,
    });
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 21,
        visibility: wgpu::ShaderStages::FRAGMENT,
        ty: wgpu::BindingType::Texture {
            sample_type: wgpu::TextureSampleType::Float { filterable: true },
            view_dimension: wgpu::TextureViewDimension::D2,
            multisampled: false,
        },
        count: None,
    });
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 22,
        visibility: wgpu::ShaderStages::FRAGMENT,
        ty: wgpu::BindingType::Sampler(wgpu::SamplerBindingType::Filtering),
        count: None,
    });
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 23,
        visibility: wgpu::ShaderStages::FRAGMENT,
        ty: wgpu::BindingType::Texture {
            sample_type: wgpu::TextureSampleType::Float { filterable: true },
            view_dimension: wgpu::TextureViewDimension::D2,
            multisampled: false,
        },
        count: None,
    });
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 24,
        visibility: wgpu::ShaderStages::FRAGMENT,
        ty: wgpu::BindingType::Sampler(wgpu::SamplerBindingType::Filtering),
        count: None,
    });
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 25,
        visibility: wgpu::ShaderStages::FRAGMENT,
        ty: wgpu::BindingType::Texture {
            sample_type: wgpu::TextureSampleType::Float { filterable: true },
            view_dimension: wgpu::TextureViewDimension::D2,
            multisampled: false,
        },
        count: None,
    });
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 26,
        visibility: wgpu::ShaderStages::FRAGMENT,
        ty: wgpu::BindingType::Sampler(wgpu::SamplerBindingType::Filtering),
        count: None,
    });
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 27,
        visibility: wgpu::ShaderStages::FRAGMENT,
        ty: wgpu::BindingType::Texture {
            sample_type: wgpu::TextureSampleType::Float { filterable: true },
            view_dimension: wgpu::TextureViewDimension::D2,
            multisampled: false,
        },
        count: None,
    });
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 28,
        visibility: wgpu::ShaderStages::FRAGMENT,
        ty: wgpu::BindingType::Sampler(wgpu::SamplerBindingType::Filtering),
        count: None,
    });
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 29,
        visibility: wgpu::ShaderStages::FRAGMENT,
        ty: wgpu::BindingType::Texture {
            sample_type: wgpu::TextureSampleType::Float { filterable: true },
            view_dimension: wgpu::TextureViewDimension::D2,
            multisampled: false,
        },
        count: None,
    });
    entries.push(wgpu::BindGroupLayoutEntry {
        binding: 30,
        visibility: wgpu::ShaderStages::FRAGMENT,
        ty: wgpu::BindingType::Sampler(wgpu::SamplerBindingType::Filtering),
        count: None,
    });
}

/// Returns `(layout, scene_uniform_size_bytes)` for WGSL `@group(1)` clustered-forward scene data.
pub fn create_renderide_scene_bind_group_layout(device: &wgpu::Device, label: &str) -> (wgpu::BindGroupLayout, u64) {
    let mut entries = Vec::new();
    push_renderide_scene_bind_group_layout_entries(&mut entries);
    let scene_uniform_size = std::mem::size_of::<crate::gpu::pipeline::SceneUniforms>() as u64;
    let layout = device.create_bind_group_layout(&wgpu::BindGroupLayoutDescriptor {
        label: Some(label),
        entries: &entries,
    });
    (layout, scene_uniform_size)
}

pub fn create_material_bind_group_layout(device: &wgpu::Device, label: &str) -> wgpu::BindGroupLayout {
    let mut entries = Vec::new();
    push_material_bind_group_layout_entries(&mut entries);
    device.create_bind_group_layout(&wgpu::BindGroupLayoutDescriptor {
        label: Some(label),
        entries: &entries,
    })
}

/// `texture_views_and_samplers` must contain one `(view, sampler)` per texture property in Properties order.
pub fn create_material_bind_group(
    device: &wgpu::Device,
    layout: &wgpu::BindGroupLayout,
    label: &str,
    material_uniform_buffer: &wgpu::Buffer,
    texture_views_and_samplers: &[(&wgpu::TextureView, &wgpu::Sampler)],
) -> wgpu::BindGroup {
    let mut entries: Vec<wgpu::BindGroupEntry> = Vec::new();
    entries.push(wgpu::BindGroupEntry {
        binding: 0,
        resource: wgpu::BindingResource::Buffer(wgpu::BufferBinding {
            buffer: material_uniform_buffer,
            offset: 0,
            size: None,
        }),
    });
    let mut b: u32 = 1;
    for (view, sampler) in texture_views_and_samplers {
        entries.push(wgpu::BindGroupEntry {
            binding: b,
            resource: wgpu::BindingResource::TextureView(view),
        });
        b += 1;
        entries.push(wgpu::BindGroupEntry {
            binding: b,
            resource: wgpu::BindingResource::Sampler(sampler),
        });
        b += 1;
    }
    device.create_bind_group(&wgpu::BindGroupDescriptor { label: Some(label), layout, entries: &entries })
}

/// Primitive assembly for pass 0 (from ShaderLab `Cull`). Tags: `LightMode` = `ForwardBase`; `Queue` = `AlphaTest`; `RenderType` = `TransparentCutout`.
pub fn primitive_state_pass0() -> wgpu::PrimitiveState {
    wgpu::PrimitiveState {
        topology: wgpu::PrimitiveTopology::TriangleList,
        strip_index_format: None,
        front_face: wgpu::FrontFace::Ccw,
        cull_mode: Some(wgpu::Face::Back),
        unclipped_depth: false,
        polygon_mode: wgpu::PolygonMode::Fill,
        conservative: false,
    }
}

/// Depth/stencil for pass 0 when a depth attachment is used.
pub fn depth_stencil_state_pass0(depth_format: wgpu::TextureFormat) -> wgpu::DepthStencilState {
    wgpu::DepthStencilState {
        format: depth_format,
        depth_write_enabled: true,
        depth_compare: wgpu::CompareFunction::LessEqual,
        stencil: wgpu::StencilState {
        front: wgpu::StencilFaceState::IGNORE,
        back: wgpu::StencilFaceState::IGNORE,
        read_mask: 0,
        write_mask: 0,
    },
        bias: wgpu::DepthBiasState {
            constant: 0,
            slope_scale: 0.0,
            clamp: 0.0,
        },
    }
}

/// First color target for pass 0 (blend + write mask from ShaderLab).
pub fn color_target_state_pass0(surface_format: wgpu::TextureFormat) -> Option<wgpu::ColorTargetState> {
    Some(wgpu::ColorTargetState {
                format: surface_format,
                blend: None,
                write_mask: wgpu::ColorWrites::ALL,
            })
}

/// Primitive assembly for pass 1 (from ShaderLab `Cull`). Tags: `LightMode` = `ForwardAdd`; `Queue` = `AlphaTest`; `RenderType` = `TransparentCutout`.
pub fn primitive_state_pass1() -> wgpu::PrimitiveState {
    wgpu::PrimitiveState {
        topology: wgpu::PrimitiveTopology::TriangleList,
        strip_index_format: None,
        front_face: wgpu::FrontFace::Ccw,
        cull_mode: Some(wgpu::Face::Back),
        unclipped_depth: false,
        polygon_mode: wgpu::PolygonMode::Fill,
        conservative: false,
    }
}

/// Depth/stencil for pass 1 when a depth attachment is used.
pub fn depth_stencil_state_pass1(depth_format: wgpu::TextureFormat) -> wgpu::DepthStencilState {
    wgpu::DepthStencilState {
        format: depth_format,
        depth_write_enabled: true,
        depth_compare: wgpu::CompareFunction::LessEqual,
        stencil: wgpu::StencilState {
        front: wgpu::StencilFaceState::IGNORE,
        back: wgpu::StencilFaceState::IGNORE,
        read_mask: 0,
        write_mask: 0,
    },
        bias: wgpu::DepthBiasState {
            constant: 0,
            slope_scale: 0.0,
            clamp: 0.0,
        },
    }
}

/// First color target for pass 1 (blend + write mask from ShaderLab).
pub fn color_target_state_pass1(surface_format: wgpu::TextureFormat) -> Option<wgpu::ColorTargetState> {
    Some(wgpu::ColorTargetState {
                format: surface_format,
                blend: Some(wgpu::BlendState {
                    color: wgpu::BlendComponent {
                        src_factor: wgpu::BlendFactor::One,
                        dst_factor: wgpu::BlendFactor::One,
                        operation: wgpu::BlendOperation::Add,
                    },
                    alpha: wgpu::BlendComponent {
                        src_factor: wgpu::BlendFactor::One,
                        dst_factor: wgpu::BlendFactor::One,
                        operation: wgpu::BlendOperation::Add,
                    },
                }),
                write_mask: wgpu::ColorWrites::ALL,
            })
}

/// Primitive assembly for pass 2 (from ShaderLab `Cull`). Tags: `LightMode` = `ShadowCaster`; `Queue` = `AlphaTest`; `RenderType` = `TransparentCutout`.
pub fn primitive_state_pass2() -> wgpu::PrimitiveState {
    wgpu::PrimitiveState {
        topology: wgpu::PrimitiveTopology::TriangleList,
        strip_index_format: None,
        front_face: wgpu::FrontFace::Ccw,
        cull_mode: Some(wgpu::Face::Back),
        unclipped_depth: false,
        polygon_mode: wgpu::PolygonMode::Fill,
        conservative: false,
    }
}

/// Depth/stencil for pass 2 when a depth attachment is used.
pub fn depth_stencil_state_pass2(depth_format: wgpu::TextureFormat) -> wgpu::DepthStencilState {
    wgpu::DepthStencilState {
        format: depth_format,
        depth_write_enabled: true,
        depth_compare: wgpu::CompareFunction::LessEqual,
        stencil: wgpu::StencilState {
        front: wgpu::StencilFaceState::IGNORE,
        back: wgpu::StencilFaceState::IGNORE,
        read_mask: 0,
        write_mask: 0,
    },
        bias: wgpu::DepthBiasState {
            constant: 0,
            slope_scale: 0.0,
            clamp: 0.0,
        },
    }
}

/// First color target for pass 2 (blend + write mask from ShaderLab).
pub fn color_target_state_pass2(surface_format: wgpu::TextureFormat) -> Option<wgpu::ColorTargetState> {
    Some(wgpu::ColorTargetState {
                format: surface_format,
                blend: None,
                write_mask: wgpu::ColorWrites::ALL,
            })
}

/// Builds a render pipeline for pass 0 using generated fixed-function state.
pub fn create_render_pipeline_pass0(
    device: &wgpu::Device,
    label: &str,
    layout: &wgpu::PipelineLayout,
    surface_format: wgpu::TextureFormat,
    depth_format: Option<wgpu::TextureFormat>,
    variant: &VariantKey,
    vertex_layout_override: Option<&[wgpu::VertexBufferLayout]>,
) -> wgpu::RenderPipeline {
    let vertex_buffers: &[wgpu::VertexBufferLayout] = match vertex_layout_override { Some(b) => b, None => VERTEX_BUFFER_LAYOUTS_PASS0 };
    let module = device.create_shader_module(wgpu::ShaderModuleDescriptor {
        label: Some("Xiexe/Toon2.0/XSToon2.0_CutoutA2C"),
        source: shader_source_forward(),
    });
    let vertex_opts = pipeline_compilation_options_pass0_vertex(variant);
    let fragment_opts = pipeline_compilation_options_pass0_fragment(variant);
    device.create_render_pipeline(&wgpu::RenderPipelineDescriptor {
        label: Some(label),
        layout: Some(layout),
        vertex: wgpu::VertexState {
            module: &module,
            entry_point: Some("vert"),
            buffers: vertex_buffers,
            compilation_options: vertex_opts,
        },
        fragment: Some(wgpu::FragmentState {
            module: &module,
            entry_point: Some("frag"),
            targets: &[color_target_state_pass0(surface_format)],
            compilation_options: fragment_opts,
        }),
        primitive: primitive_state_pass0(),
        depth_stencil: depth_format.map(depth_stencil_state_pass0),
        multisample: wgpu::MultisampleState::default(),
        multiview_mask: None,
        cache: None,
    })
}

/// Builds a render pipeline for pass 1 using generated fixed-function state.
pub fn create_render_pipeline_pass1(
    device: &wgpu::Device,
    label: &str,
    layout: &wgpu::PipelineLayout,
    surface_format: wgpu::TextureFormat,
    depth_format: Option<wgpu::TextureFormat>,
    variant: &VariantKey,
    vertex_layout_override: Option<&[wgpu::VertexBufferLayout]>,
) -> wgpu::RenderPipeline {
    let vertex_buffers: &[wgpu::VertexBufferLayout] = match vertex_layout_override { Some(b) => b, None => VERTEX_BUFFER_LAYOUTS_PASS1 };
    let module = device.create_shader_module(wgpu::ShaderModuleDescriptor {
        label: Some("Xiexe/Toon2.0/XSToon2.0_CutoutA2C"),
        source: shader_source_fwdadd(),
    });
    let vertex_opts = pipeline_compilation_options_pass1_vertex(variant);
    let fragment_opts = pipeline_compilation_options_pass1_fragment(variant);
    device.create_render_pipeline(&wgpu::RenderPipelineDescriptor {
        label: Some(label),
        layout: Some(layout),
        vertex: wgpu::VertexState {
            module: &module,
            entry_point: Some("vert"),
            buffers: vertex_buffers,
            compilation_options: vertex_opts,
        },
        fragment: Some(wgpu::FragmentState {
            module: &module,
            entry_point: Some("frag"),
            targets: &[color_target_state_pass1(surface_format)],
            compilation_options: fragment_opts,
        }),
        primitive: primitive_state_pass1(),
        depth_stencil: depth_format.map(depth_stencil_state_pass1),
        multisample: wgpu::MultisampleState::default(),
        multiview_mask: None,
        cache: None,
    })
}

/// Builds a render pipeline for pass 2 using generated fixed-function state.
pub fn create_render_pipeline_pass2(
    device: &wgpu::Device,
    label: &str,
    layout: &wgpu::PipelineLayout,
    surface_format: wgpu::TextureFormat,
    depth_format: Option<wgpu::TextureFormat>,
    variant: &VariantKey,
    vertex_layout_override: Option<&[wgpu::VertexBufferLayout]>,
) -> wgpu::RenderPipeline {
    let vertex_buffers: &[wgpu::VertexBufferLayout] = match vertex_layout_override { Some(b) => b, None => VERTEX_BUFFER_LAYOUTS_PASS2 };
    let module = device.create_shader_module(wgpu::ShaderModuleDescriptor {
        label: Some("Xiexe/Toon2.0/XSToon2.0_CutoutA2C"),
        source: shader_source_shadow_caster(),
    });
    let vertex_opts = pipeline_compilation_options_pass2_vertex(variant);
    let fragment_opts = pipeline_compilation_options_pass2_fragment(variant);
    device.create_render_pipeline(&wgpu::RenderPipelineDescriptor {
        label: Some(label),
        layout: Some(layout),
        vertex: wgpu::VertexState {
            module: &module,
            entry_point: Some("vertShadowCaster"),
            buffers: vertex_buffers,
            compilation_options: vertex_opts,
        },
        fragment: Some(wgpu::FragmentState {
            module: &module,
            entry_point: Some("fragShadowCaster"),
            targets: &[color_target_state_pass2(surface_format)],
            compilation_options: fragment_opts,
        }),
        primitive: primitive_state_pass2(),
        depth_stencil: depth_format.map(depth_stencil_state_pass2),
        multisample: wgpu::MultisampleState::default(),
        multiview_mask: None,
        cache: None,
    })
}

/// Dispatches to `create_render_pipeline_passN` by index. Panics if `pass_index` is out of range.
#[allow(clippy::too_many_arguments)]
pub fn create_render_pipeline(
    device: &wgpu::Device,
    label: &str,
    layout: &wgpu::PipelineLayout,
    surface_format: wgpu::TextureFormat,
    depth_format: Option<wgpu::TextureFormat>,
    variant: &VariantKey,
    pass_index: usize,
    vertex_layout_override: Option<&[wgpu::VertexBufferLayout]>,
) -> wgpu::RenderPipeline {
    match pass_index {
        0 => create_render_pipeline_pass0(device, label, layout, surface_format, depth_format, variant, vertex_layout_override),
        1 => create_render_pipeline_pass1(device, label, layout, surface_format, depth_format, variant, vertex_layout_override),
        2 => create_render_pipeline_pass2(device, label, layout, surface_format, depth_format, variant, vertex_layout_override),
        _ => panic!("invalid pass_index for this shader module"),
    }
}
/// Vertex shader entry name produced by Slang for pass 0.
pub const VERTEX_ENTRY_PASS0: &str = "vert";
pub const FRAGMENT_ENTRY_PASS0: &str = "frag";
/// First `#pragma target` in pass 0 (x10 for integer const).
pub const PRAGMA_SHADER_TARGET_PASS0: u32 = 30;

/// Vertex shader entry name produced by Slang for pass 1.
pub const VERTEX_ENTRY_PASS1: &str = "vert";
pub const FRAGMENT_ENTRY_PASS1: &str = "frag";
/// First `#pragma target` in pass 1 (x10 for integer const).
pub const PRAGMA_SHADER_TARGET_PASS1: u32 = 30;

/// Vertex shader entry name produced by Slang for pass 2.
pub const VERTEX_ENTRY_PASS2: &str = "vertShadowCaster";
pub const FRAGMENT_ENTRY_PASS2: &str = "fragShadowCaster";
/// First `#pragma target` in pass 2 (x10 for integer const).
pub const PRAGMA_SHADER_TARGET_PASS2: u32 = 30;

/// Pass names from ShaderLab (for future pipeline / file naming).
pub const PASS_0_NAME: &str = "FORWARD";
pub const PASS_1_NAME: &str = "FWDADD";
pub const PASS_2_NAME: &str = "ShadowCaster";
